workflows:
  android-workflow:
    name: Android Build
    max_build_duration: 120
    instance_type: mac_mini_m2

    environment:
      java: 17

    triggering:
      events:
        - push

    scripts:
      - name: Generate missing resources and XML icons
        script: |
          echo "Генерация системных XML-файлов и адаптивных иконок..."
          RES_DIR="app/src/main/res"
          mkdir -p "$RES_DIR/xml"
          mkdir -p "$RES_DIR/mipmap-anydpi-v26"

          cat <<EOF > "$RES_DIR/xml/backup_rules.xml"
          <?xml version="1.0" encoding="utf-8"?>
          <full-backup-content />
          EOF

          cat <<EOF > "$RES_DIR/xml/data_extraction_rules.xml"
          <?xml version="1.0" encoding="utf-8"?>
          <data-extraction-rules>
              <cloud-backup />
              <device-transfer />
          </data-extraction-rules>
          EOF

          cat <<EOF > "$RES_DIR/mipmap-anydpi-v26/ic_launcher.xml"
          <?xml version="1.0" encoding="utf-8"?>
          <adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
              <background>
                  <color android:color="#FFFFFF"/>
              </background>
              <foreground>
                  <vector android:height="108dp" android:width="108dp" android:viewportHeight="108" android:viewportWidth="108">
                      <path android:fillColor="#000000" android:pathData="M0,0h108v108h-108z"/>
                  </vector>
              </foreground>
          </adaptive-icon>
          EOF

          cp "$RES_DIR/mipmap-anydpi-v26/ic_launcher.xml" "$RES_DIR/mipmap-anydpi-v26/ic_launcher_round.xml"
          
          # Очищаем старые PNG-иконки, если они есть, чтобы избежать конфликтов
          rm -f $RES_DIR/mipmap-*dpi/ic_launcher*.png 2>/dev/null || true

      - name: Safe Remove Tab Indicator Parameter
        script: |
          echo "Очистка проблемного параметра indicator из MainScreen.kt..."
          MAIN_SCREEN="app/src/main/java/com/amanat/ui/screens/MainScreen.kt"
          if [ -f "$MAIN_SCREEN" ]; then
            sed -i '' '/tabIndicatorOffset/d' "$MAIN_SCREEN" 2>/dev/null || sed -i '/tabIndicatorOffset/d' "$MAIN_SCREEN"
            echo "Файл успешно исправлен."
          else
            echo "Предупреждение: Файл MainScreen.kt не найден по пути $MAIN_SCREEN"
          fi

      - name: Build APK
        script: |
          echo "Настройка прав доступа для сборщика..."
          chmod +x gradlew 2>/dev/null || true
          
          echo "Запуск компиляции приложения..."
          if [ -f "./gradlew" ]; then
            ./gradlew assembleRelease
          else
            echo "Локальный gradlew не найден, используем глобальный Gradle..."
            gradle assembleRelease
          fi

    artifacts:
      - app/build/outputs/apk/release/*.apk